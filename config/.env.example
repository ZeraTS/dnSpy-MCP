# ── Required ──────────────────────────────────────────────────────────────────
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
DNSPY_API_KEY=

# ── Network ───────────────────────────────────────────────────────────────────
DNSPY_DAEMON_PORT=9001
DNSPY_HOST=0.0.0.0          # 0.0.0.0 for hardened/firewalled host, 127.0.0.1 for local only

# ── Daemon behaviour ──────────────────────────────────────────────────────────
DNSPY_WORKER_POOL_SIZE=5
DNSPY_REQUEST_TIMEOUT=120
DNSPY_MAX_BINARY_SIZE_MB=1024
DNSPY_HEALTH_PUBLIC=false    # true = /health and /metrics don't require API key
DNSPY_LOG_LEVEL=INFO
DNSPY_CONFIG_PATH=./config/config.json

# ── Security: restrict binary paths to these colon-separated dirs (optional) ──
# DNSPY_ALLOWED_DIRS=/opt/targets:/data/binaries

# ── CLI tool (C# / ICSharpCode.Decompiler) ────────────────────────────────────
DNSPY_CLI_PATH=/opt/dnspy-mcp/cli-debugger/bin/Release/net8.0/dnspy-mcp.dll
DNSPY_CLI_TIMEOUT=60
DOTNET_PATH=dotnet

# ── Legacy: dnSpy.exe path (only needed if using GUI-based features) ──────────
DNSPY_PATH=/opt/dnspy/dnSpy.exe

# ── MCP server → daemon connection ────────────────────────────────────────────
DNSPY_DAEMON_URL=http://localhost:9001
